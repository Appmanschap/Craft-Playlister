{% extends '_layouts/elementindex' %}
{% set elementType = 'appmanschap\\youtubeplaylistimporter\\elements\\Playlist' %}

{% block actionButton %}
    <div class="buttons">
        <form method="POST">
            {{ csrfInput() }}
            {{ actionInput('youtube-playlist-importer/playlist/create') }}
            <input type="hidden" name="elementType" value="{{ elementType }}" />
            <button type="submit" class="btn submit">{{ 'New playlist' | t('youtubeplaylistimporter') }}</button>
        </form>
    </div>
{% endblock %}
